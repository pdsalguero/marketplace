generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  ads       Ad[]
  name      String?   // 👈 nuevo campo opcional
}

enum Category {
  AUTOS
  INMUEBLES
  ELECTRONICA
  HOGAR
  EMPLEO
  SERVICIOS
  MODA
  MASCOTAS
  OTROS
}

model Ad {
  id          Int       @id @default(autoincrement())
  title       String
  description String
  price       Float
  imageKey    String?
  imageKeys   Json?
  category    Category  @default(OTROS)  // ⬅️ nuevo
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}


